 # #1. modulus switching
    # # 2^60 -> 2^40
    # q_prime = pow(2, 40)
    # t = pow(2, 20)
    # T = 2
    # # ring q' 생성
    # fv_mod_q_prime = create_ring(q_prime, t, T)

    # enc_plaintext1_modulus_switched = modulus_switching(enc_plaintext1, q, q_prime)